export const metadata = {
  title: "EduBot Admission | Admin Dashboard",
  description: "Internal lead management dashboard for EduBot Admission counsellors.",
};

export default function AdminLayout({ children }) {
  return children;
}
